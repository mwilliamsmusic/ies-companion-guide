import React from 'react';
import styles from './DrawerToggleButton.module.css';

const drawerToggleButton = (props) => (
<div className={styles.drawerToggle} onClick={props.clicked}> {/*Fix me*/}

    <div></div>
    <div></div>
    <div></div>


</div>

);

export default drawerToggleButton;